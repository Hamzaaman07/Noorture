---
/**
 * Page section: the width, rhythm and optional tint every block shares.
 */
/*
  Note on `class`: Astro does not push a parent component's style scope onto a
  child component's root element, so a class passed in here is NOT reachable
  from the caller's scoped CSS — `.thing { ... }` in the parent silently
  matches nothing. Style your own wrapper inside the slot instead.
*/
interface Props {
  /** Pale aqua alternating tint (--noor-bg-alt), spec §6. */
  tint?: boolean;
  /** Narrow measure for long-form prose. */
  narrow?: boolean;
  id?: string;
  class?: string;
  labelledby?: string;
}
const {
  tint = false,
  narrow = false,
  id,
  class: className = '',
  labelledby,
} = Astro.props;
---

<section
  id={id}
  aria-labelledby={labelledby}
  class:list={['section', tint && 'section--tint', className]}
>
  <div class:list={['section__inner', narrow && 'section__inner--narrow']}>
    <slot />
  </div>
</section>

<style>
  .section {
    padding-block: clamp(4rem, 10vw, 7rem);
  }

  /*
    A near-neutral tint, not a mint one. Alternating pink and green washes made
    the page oscillate between two candy colours; a single warm grey lets the
    brand colour mean something when it does appear.
  */
  .section--tint {
    background: color-mix(in srgb, var(--color-noor-bg-alt) 70%, transparent);
    border-block: 1px solid var(--color-noor-line);
  }

  .section__inner {
    max-width: 72rem;
    margin: 0 auto;
    padding-inline: clamp(1rem, 4vw, 2.5rem);
  }

  .section__inner--narrow {
    max-width: 44rem;
  }
</style>
