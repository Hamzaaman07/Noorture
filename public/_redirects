# Cloudflare Pages redirects. One rule per line: <from> <to> <status>
#
# STAGE 4 (spec §5.4). This file is deliberately empty of rules right now.
# Before the apex is pointed here, every Podia URL currently in circulation has
# to be inventoried — Instagram bio, past emails, gift cards already issued,
# member bookmarks — and anything that moves gets a rule below. The exit
# criterion is that no URL which worked before launch is broken after it.
#
# Past members reaching their cohort archives is the case that must not break.
#
# Example:
#   /womens-noorture-circle-spring-2026  https://circles.noorture.com/womens-noorture-circle-spring-2026  301
#
# Limits: 2,000 static rules, 100 dynamic (splat/placeholder) rules.
